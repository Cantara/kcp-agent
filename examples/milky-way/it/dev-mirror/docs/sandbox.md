# Sandbox

- Order intake: `https://sandbox.melkeveien.example/orders` (mock MES,
  accepts everything, produces synthetic batch reports every 5 minutes)
- Reset: `POST /sandbox/reset` — wipes all synthetic orders
- Mock data never contains real formulations, prices or personal data.
